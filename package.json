{
  "name": "gateway-builder-standalone",
  "version": "1.0.0",
  "description": "Industrial Protocol Gateway Configuration Tool",
  "homepage": "https://SpencerEVS.github.io/gateway-builder-standalone",
  "private": true,
  "dependencies": {
    "@types/jszip": "^3.4.1",
    "@types/node": "^16.18.0",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "jszip": "^3.10.1",
    "modbus-serial": "^8.0.23",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.1",
    "typescript": "^4.9.0"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && set PORT=3001 && react-scripts start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
